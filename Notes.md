**EXPORT**

_default: export_

    const a='ali'

    export default a -> only a will export from file
        import in this scenario

        import anyname/not only a from './app'
        console.log(anyname)

*name: export*
export const a="ali"

import {a} from './app.js'


**npm create vite**
**npm run dev**
**rafce**


**Git and GitHub**
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/tuqeer6/React.git
git push -u origin main